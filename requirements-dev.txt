# ai-record — test/dev dependencies.
# These are sufficient to run the full unit + integration test suite on CPU with
# NO GPU, NO audio hardware, and NO model downloads.

numpy>=1.26
fastapi>=0.111
uvicorn>=0.30
httpx>=0.27          # FastAPI TestClient transport
pytest>=8.0
pytest-asyncio>=0.23
soundfile>=0.12      # WAV fixture assertions (optional; tests fall back to stdlib wave)
